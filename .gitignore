node_modules
dist
.DS_Store
*.log
.vite
.env
.env.local
*.tsbuildinfo

# Packed distributable produced by @conjureos/pack — a build artifact, not source.
*.conj

# esbuild dev server output (conj-pack dev)
.devserve

# Catalog build cache (the scraped dump + peeks; the built src/data/catalog.json IS committed)
scripts/.cache
