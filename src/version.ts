// App version, kept in sync with package.json. Plain source (no Vite/esbuild
// define) so the app builds the same way ConjureOS bundles a user-published
// app. Update this when you bump `version` in package.json.
export const APP_VERSION = "0.9.3";
